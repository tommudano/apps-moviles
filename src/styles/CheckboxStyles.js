import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    checkbox: {
        height: "100%",
        width: "100%",
        borderColor: "#000000",
        borderWidth: 1
    }
})

export default styles