import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    logContainer: {
        marginVertical: 10,
    },
    logContent: {
        fontFamily: "Roboto",
        fontSize: 15,
        fontWeight: "700",
    },
});

export default styles;
