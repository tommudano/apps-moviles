import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    commentContainer: {
        marginVertical: 5,
    },
    comment: {
        fontWeight: "700",
        fontStyle: "italic",
    },
});

export default styles;
